# v196 — Mobile Menu Color Parity

- Removed the separate colored shell applied only while the three-line Menu control was open.
- All five bottom-navigation controls now keep the same cyan/navy background, border, text, icon color, typography, and touch treatment.
- Active destinations use only the shared top indicator and a subtle depth change, so selection is clear without changing the button color.
- Matched the top mobile hamburger control to the same navigation design system.
- No Supabase migration is required.
