Trading Docks Admin Users v60

1. Extract this ZIP directly into the root of the current Trading Docks repository.
2. Allow the included files to replace matching files.
3. In Supabase, open SQL Editor and run:
   supabase/migrations/202607270007_admin_user_directory_usage.sql
4. Commit and push the changed files.
5. Wait for the production deployment to finish, then refresh the dashboard.

Expected source changes:
- src/components/dashboard/admin/AdminControlCenterWithPreview.tsx
- src/components/dashboard-v2/inventory/InventoryWorkspace.tsx
- src/lib/effective-plan.ts
- supabase/migrations/202607270007_admin_user_directory_usage.sql

Important:
- The owner account tradingdocks@gmail.com defaults to Business access.
- The existing Admin Plan Preview can still temporarily emulate lower plans.
- Card totals synchronize when each user opens their Inventory workspace after
  this update is deployed. Accounts that have not opened the updated inventory
  yet initially show zero cards.
