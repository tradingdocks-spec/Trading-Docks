import { money, type Receipt } from './domain.ts';
const escape = (value: unknown) => String(value ?? '').replace(/[&<>"']/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[char]!);
export function renderReceipt(r: Receipt) {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Receipt ${escape(r.number)}</title><style>@page{size:80mm 200mm;margin:4mm}*{box-sizing:border-box}body{font:12px monospace;color:#111;background:white;max-width:72mm;margin:12px auto}h1{font-size:18px}table{width:100%;border-collapse:collapse}td{padding:6px 0;border-bottom:1px solid #ddd}td:last-child{text-align:right}small{display:block}p{overflow-wrap:anywhere}.total{font-size:18px;font-weight:bold}@media print{.controls{display:none}body{margin:0}tr{break-inside:avoid}}</style></head><body><div class="controls"><button id="print">Print receipt</button></div><h1>${escape(r.site)}</h1><p>Trading Docks · ${escape(r.register)}</p><p>${escape(r.number)}<br>${escape(r.createdAt)}</p><table><tbody>${r.lines.map(l => `<tr><td>${escape(l.name)}<small>${escape([l.setCode,l.collectorNumber,l.condition,l.finish,l.language].filter(Boolean).join(' · '))}</small><small>${l.quantity} × ${money(l.unitPriceMinor)}</small></td><td>${money(l.lineTotalMinor)}</td></tr>`).join('')}</tbody></table><p>Subtotal ${money(r.subtotalMinor)}<br>Discount −${money(r.discountMinor)}<br>Tax ${money(r.taxMinor)}</p><p class="total">Total ${money(r.totalMinor)}</p><p>Cash ${money(r.cashMinor)}<br>Change ${money(r.changeMinor)}</p><p>Thank you for shopping with us.</p><script>document.getElementById('print').addEventListener('click',()=>window.print())</script></body></html>`;
}
