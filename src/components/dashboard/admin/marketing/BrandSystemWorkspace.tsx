"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { Check, Palette, Save, Sparkles } from "lucide-react";
import { DEFAULT_MARKETING_BRAND_TOKENS, type MarketingBrandTokens } from "@/lib/marketing/brand-system";

type Profile = { brandProfileVersion: number; tokens: MarketingBrandTokens; copyRules: { voice?: string[]; preferredPhrases?: string[]; prohibitedPhrases?: string[] }; logoUsageRules: { preferredVariants?: string[]; minimumSizing?: string; prohibited?: string[] }; personality?: string[]; goldStandards?: Array<{ id: string; name: string; platform: string; concept_direction?: string }> };

const swatches: Array<[keyof MarketingBrandTokens["colors"], string]> = [["backgroundPrimary", "Primary background"], ["backgroundSecondary", "Secondary surface"], ["surface", "Surface"], ["accent", "Accent"], ["textPrimary", "Primary text"], ["textSecondary", "Secondary text"], ["border", "Border"]];

export function BrandSystemWorkspace() {
  const [profile, setProfile] = useState<Profile | null>(null); const [tokens, setTokens] = useState(DEFAULT_MARKETING_BRAND_TOKENS); const [message, setMessage] = useState(""); const [saving, setSaving] = useState(false);
  useEffect(() => { void fetch("/api/admin/marketing/brand-system", { cache: "no-store" }).then((response) => response.json()).then((body) => { if (body.profile) { setProfile(body.profile); setTokens(body.profile.tokens); } else setMessage(body.error ?? "Brand System could not load."); }); }, []);
  async function save() { setSaving(true); setMessage(""); const response = await fetch("/api/admin/marketing/brand-system", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ tokens, copyRules: profile?.copyRules, logoUsageRules: profile?.logoUsageRules, personality: profile?.personality }) }); const body = await response.json(); setMessage(response.ok ? `Saved Brand System version ${body.profile.brand_profile_version}.` : body.error ?? "Brand System could not be saved."); if (response.ok) setProfile((current) => current ? { ...current, brandProfileVersion: body.profile.brand_profile_version } : current); setSaving(false); }
  return <div className="mx-auto max-w-[1500px] px-4 py-7 sm:px-8 sm:py-10"><div className="flex flex-col gap-5 border-b border-td-ink/10 pb-7 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-td-accent">Admin growth engine</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.03em] text-td-primary">Brand System</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-td-secondary">The source of truth for recognizable, product-authentic Trading Docks marketing. Every approved creative keeps the brand version it used.</p></div><div className="flex gap-2"><Link href="/dashboard/admin/marketing/creative-studio" className="td-button-secondary"><Sparkles className="h-4 w-4" />Creative Studio</Link><button type="button" onClick={() => void save()} disabled={saving} className="td-button-primary"><Save className="h-4 w-4" />{saving ? "Saving…" : "Save tokens"}</button></div></div>{message ? <p role="status" className="mt-5 rounded-xl bg-td-accent/10 p-4 text-sm text-td-accent">{message}</p> : null}<div className="mt-7 grid gap-6 xl:grid-cols-[1.15fr_.85fr]"><section className="space-y-6"><div className="rounded-2xl border border-td-ink/10 bg-td-surface/60 p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-td-accent">Identity</p><h2 className="mt-2 text-xl font-semibold text-td-primary">Trading Docks Brand Signature</h2><p className="mt-2 text-sm leading-6 text-td-secondary">Dark product surfaces, cyan action treatment, authentic UI, concise TCG-native copy, and disciplined spacing should work together before the logo is even read.</p></div><div className="rounded-xl bg-td-canvas px-4 py-3 text-right"><p className="text-xs text-td-muted">Version</p><p className="mt-1 font-mono text-lg text-td-primary">{profile?.brandProfileVersion ?? 1}</p></div></div><div className="mt-6 grid gap-3 sm:grid-cols-2"><div className="rounded-xl bg-td-canvas p-4"><p className="text-xs uppercase tracking-[.15em] text-td-muted">Preferred logos</p><p className="mt-2 text-sm text-td-secondary">Wordmark · Mark · Light · Dark · Monochrome</p></div><div className="rounded-xl bg-td-canvas p-4"><p className="text-xs uppercase tracking-[.15em] text-td-muted">Prohibited treatment</p><p className="mt-2 text-sm text-td-secondary">Stretching · skewing · rotation · glow · unapproved recolor</p></div></div></div><div className="rounded-2xl border border-td-ink/10 bg-td-surface/60 p-6"><div className="flex items-center gap-3"><Palette className="h-5 w-5 text-td-accent" /><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-td-accent">Color system</p><h2 className="mt-1 text-xl font-semibold text-td-primary">Derived from Trading Docks UI tokens</h2></div></div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{swatches.map(([key, label]) => <label key={key} className="rounded-xl border border-td-ink/10 bg-td-canvas p-3"><span className="flex items-center gap-3"><input type="color" value={tokens.colors[key]} onChange={(event) => setTokens((current) => ({ ...current, colors: { ...current.colors, [key]: event.target.value } }))} className="h-9 w-9 cursor-pointer rounded-lg border-0 bg-transparent p-0" /><span><span className="block text-sm font-medium text-td-primary">{label}</span><span className="font-mono text-xs text-td-muted">{tokens.colors[key]}</span></span></span></label>)}</div></div><div className="rounded-2xl border border-td-ink/10 bg-td-surface/60 p-6"><p className="text-xs font-semibold uppercase tracking-[.18em] text-td-accent">Typography and voice</p><div className="mt-4 grid gap-4 md:grid-cols-2"><div><p className="text-4xl font-semibold text-td-primary">Display / Headline</p><p className="mt-3 text-sm leading-6 text-td-secondary">{tokens.typography.headlineFamily}</p></div><div><p className="text-sm leading-6 text-td-secondary">Concise, confident, TCG-native, operator-aware, understated, and direct.</p><p className="mt-3 text-xs text-td-muted">Avoid hype, fake urgency, generic SaaS language, unsupported claims, and emoji-heavy advertising.</p></div></div></div></section><aside className="space-y-6"><div className="rounded-2xl border border-td-accent/20 bg-td-accent/[.04] p-6"><p className="text-xs font-semibold uppercase tracking-[.18em] text-td-accent">Quality gate</p><h2 className="mt-2 text-xl font-semibold text-td-primary">Brand Signature</h2><div className="mt-5 space-y-3 text-sm text-td-secondary"><p><Check className="mr-2 inline h-4 w-4 text-td-success" />Approved logo and palette</p><p><Check className="mr-2 inline h-4 w-4 text-td-success" />Authentic product UI or approved TCG proof</p><p><Check className="mr-2 inline h-4 w-4 text-td-success" />Feature-specific copy and approved CTA</p><p><Check className="mr-2 inline h-4 w-4 text-td-success" />Safe margins, readable type, platform dimensions</p></div><p className="mt-5 text-xs leading-5 text-td-muted">Approval uses pass, warning, and fail checks. It does not invent a meaningless numeric score.</p></div><div className="rounded-2xl border border-td-ink/10 bg-td-surface/60 p-6"><p className="text-xs font-semibold uppercase tracking-[.18em] text-td-accent">Gold Standard Library</p><h2 className="mt-2 text-xl font-semibold text-td-primary">Approved references</h2><p className="mt-2 text-sm leading-6 text-td-muted">Approved creatives can be marked as structural references without copying them pixel-for-pixel.</p><div className="mt-5 space-y-3">{profile?.goldStandards?.length ? profile.goldStandards.map((item) => <div key={item.id} className="rounded-xl bg-td-canvas p-3"><p className="font-medium text-td-primary">{item.name}</p><p className="mt-1 text-xs text-td-muted">{item.platform} · {item.concept_direction ?? "brand reference"}</p></div>) : <p className="rounded-xl border border-dashed border-td-ink/15 p-4 text-sm text-td-muted">No Gold Standard creatives yet. Approve a strong product-first creative, then promote it from Creative Studio.</p>}</div></div></aside></div></div>;
}
