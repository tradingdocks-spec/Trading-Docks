"use client";
import { useEffect, useState } from "react";
import { ShowcasePublicExperience } from "@/components/showcase/ShowcasePublicExperience";
import type { ShowcaseCard } from "@/lib/showcase";

export function KioskExperience() { const [state, setState] = useState<{ profile: Record<string, any>; cards: ShowcaseCard[]; slug: string } | null>(null); const [invalid, setInvalid] = useState(false); const [resetKey, setResetKey] = useState(0); useEffect(() => { let active = true; const query = window.location.search; void fetch(`/api/showcase/kiosk${query}`, { cache: "no-store" }).then(async (response) => { if (!response.ok) { if (active) setInvalid(true); return; } const data = await response.json(); if (active) setState({ profile: data.profile, cards: data.cards, slug: data.kiosk.showcase_slug }); }); return () => { active = false; }; }, [resetKey]); useEffect(() => { const timer = window.setTimeout(() => setResetKey((value) => value + 1), 5 * 60 * 1000); return () => window.clearTimeout(timer); }, [resetKey]); if (invalid) return <div className="flex min-h-screen items-center justify-center bg-[#071017] p-6 text-center text-white"><div><h1 className="text-2xl font-semibold">This kiosk session has ended.</h1><button onClick={() => { setInvalid(false); setState(null); setResetKey((value) => value + 1); }} className="mt-5 rounded-xl bg-cyan-300 px-5 py-3 font-bold text-slate-950">Pair again</button></div></div>; if (!state) return <div className="flex min-h-screen items-center justify-center bg-[#071017] text-cyan-200">Loading Showcase…</div>; return <ShowcasePublicExperience key={resetKey} slug={state.slug} profile={state.profile} cards={state.cards} initialQuery={new URLSearchParams(window.location.search).get("q") ?? ""} kioskMode onStartOver={() => { window.history.replaceState(null, "", "/kiosk"); setResetKey((value) => value + 1); }} />; }
