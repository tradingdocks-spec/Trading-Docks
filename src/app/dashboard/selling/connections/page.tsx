import { SellingPlaceholder } from "@/components/dashboard/selling/SellingPlaceholder";
export default function SellingConnectionsPage() { return <SellingPlaceholder title="Marketplace connections" description="Connection health will normalize existing marketplace accounts and expose only the capabilities each adapter actually supports." />; }
