"use client";
import { useState } from "react";
import { MonitorSmartphone, Sparkles } from "lucide-react";

export default function KioskPairPage() { const [code, setCode] = useState(""); const [name, setName] = useState("Front Counter"); const [message, setMessage] = useState<string | null>(null); async function pair() { setMessage("Pairing…"); const response = await fetch("/api/showcase/kiosks/pair", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ action: "consume", code, name }) }); const result = await response.json(); if (!response.ok) { setMessage(result.error); return; } localStorage.setItem("td-showcase-kiosk-token", result.token); setMessage("Device paired. Open the store Showcase to begin browsing."); } return <main className="flex min-h-screen items-center justify-center bg-[#071017] px-4 text-white"><section className="w-full max-w-md rounded-3xl border border-white/10 bg-[#0b1821] p-7 shadow-2xl"><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><MonitorSmartphone /></div><p className="mt-6 text-xs font-bold uppercase tracking-[.18em] text-cyan-200">Trading Docks Showcase</p><h1 className="mt-2 text-3xl font-semibold">Pair this counter device</h1><p className="mt-2 text-sm leading-6 text-white/50">Generate a short-lived pairing code from Dashboard → Showcase → Kiosks, then enter it here.</p><label className="mt-6 block text-sm font-semibold">Device name<input value={name} onChange={(event) => setName(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-white/[.04] px-3 py-3 outline-none" /></label><label className="mt-4 block text-sm font-semibold">Pairing code<input value={code} onChange={(event) => setCode(event.target.value)} inputMode="numeric" placeholder="123 456" className="mt-2 w-full rounded-xl border border-white/10 bg-white/[.04] px-3 py-3 text-lg tracking-[.2em] outline-none" /></label><button onClick={pair} className="mt-5 w-full rounded-xl bg-cyan-300 py-3 text-sm font-bold text-slate-950">Pair device</button>{message ? <p className="mt-4 text-center text-sm text-cyan-100">{message}</p> : null}<p className="mt-7 flex items-center justify-center gap-2 text-xs text-white/30"><Sparkles className="h-3.5 w-3.5" /> Restricted Showcase access only</p></section></main>; }
