# v197 — Exact mobile menu color match

- Hard-locks the same icon and label color inside every bottom-navigation item.
- Prevents mobile browser button defaults from muting the Menu control.
- Preserves the shared shell, spacing, typography, glow, and active indicator.
- No database changes are required.
