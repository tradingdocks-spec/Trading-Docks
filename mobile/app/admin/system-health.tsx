import { Ionicons } from '@expo/vector-icons';
import { ScrollView, StyleSheet, Text, useWindowDimensions, View } from 'react-native';
import { AdminPanel, AdminTopBar } from '@/components/admin';
import { color, radius, space, type } from '@/design';
const checks=[['Supabase connection',Boolean(process.env.EXPO_PUBLIC_SUPABASE_URL&&process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY),'Authentication and database configuration'],['RevenueCat','pending','Native billing credentials and webhooks'],['Offline queue',true,'Cross-platform local storage adapter'],['Admin RBAC','database','Supabase migration and owner promotion required'],['Error reporting','pending','Connect Sentry or equivalent before production']];
export default function Health(){const {width}=useWindowDimensions();return <ScrollView style={s.page} contentContainerStyle={[s.content,width>800&&s.wide]}><AdminTopBar title="System Health" back/><View style={s.hero}><Text style={s.heroTitle}>Production readiness</Text><Text style={s.heroCopy}>Foundation checks are intentionally explicit. A green interface should never conceal an unconfigured dependency.</Text></View><AdminPanel>{checks.map(([name,state,copy])=>{const ok=state===true;const pending=state==='pending';const accent=ok?color.success:pending?color.warning:color.primaryBright;return <View key={String(name)} style={s.row}><View style={[s.icon,{backgroundColor:accent+'16'}]}><Ionicons name={ok?'checkmark':pending?'time':'server'} size={20} color={accent}/></View><View style={{flex:1}}><Text style={s.name}>{name}</Text><Text style={s.copy}>{copy}</Text></View><Text style={[s.state,{color:accent}]}>{ok?'READY':pending?'SETUP':'DATABASE'}</Text></View>})}</AdminPanel></ScrollView>}
const s=StyleSheet.create({page:{flex:1,backgroundColor:color.canvas},content:{padding:20,paddingTop:48,paddingBottom:70,gap:space.lg},wide:{maxWidth:1000,width:'100%',alignSelf:'center'},hero:{padding:22,borderRadius:radius.xl,backgroundColor:color.surfaceRaised,borderWidth:1,borderColor:color.border},heroTitle:{...type.heading,color:color.text},heroCopy:{...type.body,color:color.textSecondary,marginTop:8,maxWidth:700},row:{minHeight:82,flexDirection:'row',alignItems:'center',gap:13,padding:16,borderBottomWidth:StyleSheet.hairlineWidth,borderBottomColor:color.border},icon:{width:42,height:42,borderRadius:14,alignItems:'center',justifyContent:'center'},name:{color:color.text,fontWeight:'900'},copy:{color:color.textMuted,fontSize:11,marginTop:4},state:{fontWeight:'900',fontSize:9,letterSpacing:1}});
