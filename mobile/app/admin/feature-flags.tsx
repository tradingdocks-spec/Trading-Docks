import { ScrollView, StyleSheet, Switch, Text, useWindowDimensions, View } from 'react-native';
import { AdminPanel, AdminTopBar } from '@/components/admin';
import { color, space, type } from '@/design';
const flags=[['deal_desk_beta','Deal Desk beta','Seller and Store accounts',true],['sealed_evaluator','Sealed evaluator','Internal testing only',false],['magic_link_auth','Magic link authentication','All users',true],['qr_desktop_sync','QR desktop sync','Coming soon',false]];
export default function Flags(){const {width}=useWindowDimensions();return <ScrollView style={s.page} contentContainerStyle={[s.content,width>800&&s.wide]}><AdminTopBar title="Feature Flags" back/><Text style={s.copy}>Stage releases safely without shipping a new build for every audience change. These controls are presentation-only until connected to the feature_flags table.</Text><AdminPanel>{flags.map(([key,name,audience,enabled])=><View key={String(key)} style={s.row}><View style={{flex:1}}><Text style={s.name}>{name}</Text><Text style={s.audience}>{audience} · {key}</Text></View><Switch value={Boolean(enabled)} disabled trackColor={{false:color.border,true:color.primary}}/></View>)}</AdminPanel></ScrollView>}
const s=StyleSheet.create({page:{flex:1,backgroundColor:color.canvas},content:{padding:20,paddingTop:48,paddingBottom:70,gap:space.lg},wide:{maxWidth:950,width:'100%',alignSelf:'center'},copy:{...type.body,color:color.textSecondary,maxWidth:700},row:{minHeight:78,flexDirection:'row',alignItems:'center',gap:12,padding:16,borderBottomWidth:StyleSheet.hairlineWidth,borderBottomColor:color.border},name:{color:color.text,fontWeight:'900'},audience:{color:color.textMuted,fontSize:11,marginTop:5}});
